merge_protections_settings:
  auto_merge_conditions: true
merge_protections:
  - name: VM2 native CODEOWNERS 3000-file completeness discriminator
    if:
      - title = VM2_CODEOWNERS_3001_EXTERNAL
    success_conditions:
      - "#files = 3001"
