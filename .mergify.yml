merge_protections_settings:
  auto_merge_conditions: true
merge_protections:
  - name: VM2 CODEOWNERS 3000-file injected-condition observer
    if:
      - title = HOLD_VM2_CODEOWNERS_3001_EXTERNAL
    success_conditions:
      - "#files = 3001"
