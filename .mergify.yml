
queue_rules:
  - name: VM2 protected cleanup probe
    queue_branch_prefix: release/
    queue_conditions: []
    merge_conditions:
      - check-success = vm2-never-pass-0915
pull_request_rules:
  - name: vm2 inline trusted primary only
    conditions:
      - author = livastic-vm2-primary-20260911
      - base = main
    actions:
      comment:
        message: VM2-INLINE-TRUSTED-BASELINE
